public enum AntRole
{
    Worker,
    Soldier,
    Scout,
    Builder,
    Nurse
}

public enum AntType
{
    Worker,
    Soldier,
    Scout,
    Builder,
    Nurse
}

public enum WorkerJob
{
    Digger,
    Idle,
    Returning,
    FoodCollector,
    EggCarrier,
    WaterCollector,
    MineralCollector,
    Any
}

public enum ResourceType
{
    Food,
    Water,
    Minerals
}